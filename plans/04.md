# Goal
bb validate verifies that the ssh-agent is running using the environment variable SSH_AUTH_SOCK and the ssh key from the option :compute-pubkey is loaded

# Implementation
add a step to @src/clj/io/github/bigconfig_ai/once/validation.clj
 
