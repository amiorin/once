# once (Python)

Python implementation of the `once` infrastructure automation tool.

Runtime dependencies are stdlib-only. Development uses `uv` and `pytest`:

```bash
uv sync --dev
uv run pytest
uv run once --help
```
